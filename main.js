function preload() {
    //no items to load
}

function setup() {
    canvas = createCanvas(400, 400);
    canvas.center();
}

function draw() {
    //no code today
}

function take_snapshot() {
    save('filterImage.png');
}